/**
 * Zap feature — issue location per spec (`/frontend/src/features/zap`).
 *
 * The Vite application and implementation live under `client/src/features/zap`.
 * Re-export here so tooling and reviewers find the feature at the documented path.
 */
export * from "../../../../client/src/features/zap";
